# Active Sessions

One row per running session. `/session-start` adds it, `/session-end` removes it.

A row that stands overnight or over several days is normal. It means the session is still open,
not that something went wrong — picking a long thread back up is usually cheaper than rebuilding
the context.

| Topic | Started | Slug |
|-------|---------|------|

---

*Empty table = no session running.*
