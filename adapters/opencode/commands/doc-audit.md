---
description: Every 14 days — do the files the loop reads still tell the truth?
---
Run the `doc-audit` procedure: call the skill tool with name "doc-audit", and follow it step by step.

What the person passed with the command, if anything: $ARGUMENTS
