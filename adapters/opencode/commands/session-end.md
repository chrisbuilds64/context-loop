---
description: Close the session — write the log with its handover, record decisions, update the state, release the topic
---
Run the `session-end` procedure: call the skill tool with name "session-end", and follow it step by step.

What the person passed with the command, if anything: $ARGUMENTS
