---
description: Start a working session — load the state, declare a topic, pick up the thread
---
Run the `session-start` procedure: call the skill tool with name "session-start", and follow it
step by step.

The state of this project as of right now, read from the files before you begin:

!`bash .opencode/session-context.sh --plain`
