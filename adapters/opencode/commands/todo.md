---
description: What is due, what is next, what is with someone else, what is blocked — read-only
---
Run the `todo` procedure: call the skill tool with name "todo", and follow it step by step.

What the person passed with the command, if anything: $ARGUMENTS
