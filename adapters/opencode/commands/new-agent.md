---
description: Define a second agent, for work that needs a different stance than the one you have
---
Run the `new-agent` procedure: call the skill tool with name "new-agent", and follow it step by step.

What the person passed with the command, if anything: $ARGUMENTS
