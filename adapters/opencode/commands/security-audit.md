---
description: Every 14 days — secrets, .gitignore, history, dependencies, agent reach, key age
---
Run the `security-audit` procedure: call the skill tool with name "security-audit", and follow it step by step.

What the person passed with the command, if anything: $ARGUMENTS
